## 1. Migration Baseline and Safe Fixtures

- [x] 1.1 Record the two Metapac source inventories, without deriving entries from Infinity's installed state, and verify programmatically that they contain 237 `all` Arch names, 249 `cachyos` Arch names, zero overlap, 5 npm names, and 1 pipx name (`cachyos-package-state`: Exact migration accounting, CachyOS module selection, Desired declarations are not installed-state snapshots).
- [x] 1.2 Create one disposable fixture environment with isolated HOME/XDG/state/cache paths and harmless package, service, firewall, credential, group, mount, and `/etc` substitutes; verify a canary proves no host mutation command executes (`cachyos-bootstrap`: Infinity remains read-only; `cachyos-service-state`: Privileged mutations are explicit and approval-bounded).
- [x] 1.3 Add a synthetic CachyOS Chezmoi data set containing no real secrets and verify secret-bearing templates render only synthetic structure or an explicit omission (`cachyos-bootstrap`: Credential failure is closed; `cachyos-user-environment`: Secrets are not source state).
- [x] 1.4 Inventory every command or transport interaction with Infinity and verify each is inspection-only; verify no fixture or acceptance run targets Infinity (`cachyos-bootstrap`: Infinity remains read-only).

## 2. Declarch Package Authority

- [x] 2.1 Add the Declarch root configuration and selected `all`/`cachyos` modules using the official paru and npm backends plus the minimal pipx backend; verify both modules load and backend/package classes resolve as declared (`cachyos-package-state`: CachyOS module selection, Package backends preserve package classes).
- [x] 2.2 Remove `alacritty` from the migrated CachyOS module and implement the source-to-result comparison; verify exactly 485 retained Arch names, 5 npm names, 1 pipx name, and the sole omission `alacritty` (`cachyos-package-state`: Exact migration accounting; `cachyos-user-environment`: Alacritty is absent from desired state).
- [x] 2.3 Exercise the pipx backend against a mock executable and verify its list, install, and remove mappings without changing host pipx state (`cachyos-package-state`: Package backends preserve package classes, Safe package preview).
- [x] 2.4 Run Declarch validate/duplicate/conflict checks and dry-run sync/prune inside the fixture; verify missing-module and unexpected-package differences fail before mutation and the real package database/state hashes remain unchanged (`cachyos-package-state`: Exact migration accounting, Safe package preview, Native prune behavior is represented honestly).
- [x] 2.5 Encode the production preview workflow so unmanaged packages are reported without claiming they are prune candidates and no plain sync occurs between declaration removal and intended prune; verify command documentation and fixture plans reflect native Declarch 0.8.2 behavior (`cachyos-package-state`: Native prune behavior is represented honestly).

## 3. One-Invocation CachyOS Bootstrap

- [x] 3.1 Restrict bootstrap selection to the exact Chezmoi selector `.osid == "linux-cachyos"` while retaining hostname only for evidenced machine-specific behavior; verify an exact-match fixture is accepted and missing, `linux-arch`, and other non-match fixtures exit before package or Chezmoi mutation (`cachyos-bootstrap`: CachyOS-only bootstrap scope; `cachyos-service-state`: Machine-specific service state remains machine-specific).
- [x] 3.2 Replace Metapac bootstrap prerequisites with the minimum Declarch/Chezmoi retrieval prerequisites; install pinned Declarch 0.8.2 through its checksum-verified official prebuilt-release installer, and verify installer-checksum failure stops before apply while steady-state package intent remains solely in Declarch modules (`cachyos-bootstrap`: Minimal bootstrap prerequisites; `cachyos-package-state`: One package authority).
- [x] 3.3 Make bootstrap execute one `chezmoi init --apply` deployment path; verify one apply invocation, bootstrap/apply failure propagation, and focused rendered-phase failure propagation, while reserving full chained execution for 7.1 (`cachyos-bootstrap`: One-invocation deployment).
- [x] 3.4 Exercise existing-session, first-time account-addition, and unavailable 1Password paths without real credentials; verify the authenticated session is checked before apply, unavailable required data fails closed, and no secret-like value appears in source, fixture files, or logs (`cachyos-bootstrap`: Credential failure is closed).

## 4. Stable User Environment and Ownership

- [x] 4.1 Delete the Alacritty Chezmoi source and define intentional target cleanup; verify no managed Alacritty destination exists and `alacritty` is absent from the 485-name package result (`cachyos-user-environment`: Alacritty is absent from desired state).
- [x] 4.2 Remove Discord `settings.json` and pavucontrol UI state from Chezmoi ownership, choosing leave-in-place versus deletion explicitly for each; verify the rendered action plan matches those choices and generated state is not reconstructed (`cachyos-user-environment`: Generated application state remains application-owned).
- [x] 4.3 Audit every retained managed tool against the approved location decision and make only moves justified by documented OS-native destination differences; verify home-relative/Linux-only sources remain in home layout and no new non-CachyOS destination or compatibility shim is added (`cachyos-user-environment`: Configuration source placement follows documented destinations).
- [x] 4.4 Render the complete CachyOS user configuration with synthetic data and run available native parsers; verify shell, terminal, editor, theme, keybinding, and application destinations exist as authored while generated/cache/history paths remain absent or ignored (`cachyos-user-environment`: Stable workstation configuration, Generated application state remains application-owned).
- [x] 4.5 Assemble Hyprland configuration with fixture DMS modules and inspect its authored references; verify Chezmoi owns only the root/personal overlays and no DMS-generated module is duplicated, while reserving live compositor validation for 7.1 (`cachyos-user-environment`: Hyprland and DMS ownership remains split).

## 5. Desktop and Service State

- [x] 5.1 Replace DMS installer package side effects with package-declaration ownership and retain only version-validated DMS-native configuration/greeter behavior; verify fixture traces contain no second package installation path (`cachyos-service-state`: DMS configuration does not duplicate package installation).
- [x] 5.2 Replace the five Metapac service hooks with the smallest explicit CachyOS setup actions for greetd, Kanata, libvirt, kanata-switcher, and OpenRazer; verify the authority map has one owner per action and no Declarch hook reproduces privileged shell payloads (`cachyos-service-state`: Service intent has one authority).
- [x] 5.3 Validate user-unit routing and failure propagation with harmless systemctl substitutes; verify package/Chezmoi unit-file ownership, Chezmoi activation commands for `dms.service`, `hyprpolkitagent.service`, `kanata.service`, `kanata-switcher.service`, and `openrazer-daemon.service`, plus authored-Hyprland start-only behavior for `hyprland-session.target`; reserve live enabled/running state for 7.1 (`cachyos-service-state`: Intended user services are activatable).
- [x] 5.4 Validate system-unit setup and security-script failure propagation through fixture substitutes; verify package unit-file ownership and Chezmoi activation commands for `greetd.service`, `NetworkManager.service`, `bluetooth.service`, `sshd.service`, `ufw.service`, `cups.service`, `cups.socket`, `cups.path`, and `libvirtd.service`, including intended on-demand activation for CUPS/libvirt and no undeclared Infinity unit copied; reserve live unit state for 7.1 (`cachyos-service-state`: Intended system services are activatable, Privileged mutations are explicit and approval-bounded).
- [x] 5.5 Verify a non-Infinity CachyOS fixture omits Infinity vault mounts, symlinks, and hardware actions while retaining shared service/user state (`cachyos-service-state`: Machine-specific service state remains machine-specific).
- [x] 5.6 After bootstrap replacement (3.2–3.4), generated-state cleanup (4.1–4.5), and desktop/service replacement (5.1–5.5) pass their fixture checks, remove active Metapac config, group links, package execution, and package-editing wrapper; verify repository-wide search finds no active Metapac authority and rollback source remains available in Git history (`cachyos-package-state`: One package authority).

## 6. Integrated Verification and Production Documentation

- [x] 6.1 Run isolated non-mutating fixtures for the bootstrap entry point, rendered package phases, and rendered desktop/service/security setup, plus a complete synthetic render; verify component-local ordering, rendered outputs, injected failure propagation, one apply invocation, 485/5/1 package accounting, and zero host package/service/firewall/credential mutation, while reserving actual Chezmoi cross-script ordering and end-to-end convergence for 7.1 (all four capabilities).
- [x] 6.2 Update README to describe only the final one-command CachyOS workflow, native Declarch previews and prune limitation, approval boundaries, generated-state ownership, and rollback; verify it contains no instruction for a second `chezmoi apply` and no stale Metapac workflow.
- [x] 6.3 Run OpenSpec strict validation, available syntax/native checks, `git diff --check`, complete diff inspection, and status inspection; verify every requirement and scenario in the four capability specs has component evidence from a named task or is explicitly reserved for task 7.1.
- [x] 6.4 Inspect every changed executable/configuration path and verify the implementation contains no reconciliation wrapper, ownership database, quarantine mechanism, graph parser, evidence framework, service framework, or parallel orchestration around native tools (`cachyos-package-state`: Native ownership without parallel frameworks).

## 7. Acceptance and Checkpoint

- [ ] 7.1 With separate explicit approval, run bootstrap on a disposable minimal-CachyOS machine and verify package, user-environment, and service convergence; if approval is not given, do not run it and record end-to-end convergence as unverified (`cachyos-bootstrap`: End-to-end acceptance boundary; `cachyos-service-state`: intended user/system services).
- [x] 7.2 Run an independent read-only review of the completed increment, resolve every blocking finding, and repeat affected checks; verify the final review result is approval.
- [x] 7.3 Commit only intended OpenSpec and implementation paths, push the branch, and open the checkpoint pull request with scope, capability/task coverage, documentation, actual verification, unverified behavior, safety boundaries, and rollback; verify the remote PR URL and stop without merging or deploying to Infinity.
