export PROTON_ENABLE_WAYLAND=1
SCB_NOSCOPE=1
command+=" --launcher-skip"
[[ $(hyprctl -j monitors | jq --raw-output '.[] | select(.name == "DP-3") | .name') == DP-3 ]] || {
    SCB_AUTO_RES=1
    SCB_AUTO_VRR=1
}
