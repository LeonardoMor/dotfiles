
# Custom binds. Make sure to unbind any default binds before you add new ones.

# Example:
# unbind = SUPER, A
# bind = SUPER, A, exec, kitty

# You can also remove all the default binds with
# unbind = all

unbind = $mainMod, Q
bindd = $mainMod, T, Open terminal, exec, $terminal
unbind = $mainMod, C
bindd = $mainMod, Q, Close window, killactive,
bindd = $mainMod, Space, Open launcher, exec, $menu
bindd = CTRL ALT, Space, Switch keyboard layout, exec, layoutshoutout
bindd = $mainMod, F1, Show all binds, exec, hyprctl dispatch global hyprland-shell:shortcutsToggle
bindd = $mainMod SHIFT, COMMA, Dismiss all notifications, exec, qs ipc call notification clear
bindd = $mainMod SHIFT, A, Switch audio output, exec, wpctl set-default "$(pw-dump | get-next-sink)"
bindd = $mainMod, N, Toggle right panel, exec, hyprctl dispatch global hyprland-shell:rightMenuToggle
unbind = $mainMod, S
bind = $mainMod, Z, togglespecialworkspace, first
unbind = $mainMod SHIFT, S
bind = $mainMod SHIFT, Z, movetoworkspacesilent, special:first
unbind = , Print
bindd = , Print, Take screenshot of currently active output, exec, $screenshot -fno copysave output
bindd = $mainMod SHIFT, S, Screenshot area to clipboard, exec, $screenshot -fn copy area
bindd = $mainMod CTRL SHIFT, S, Annotate screenshot area to file, exec, GRIMBLAST_EDITOR="satty --filename" $screenshot -fnt ppm edit area
bindd = $mainMod, G, Launch Steam, exec, launch-or-focus steam
bindd = $mainMod SHIFT, V, Clipboard history, exec, $clipboard
